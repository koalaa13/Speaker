syntax = "proto3";

option go_package = "./proto";

service AudioService {
  rpc Connect (stream AudioInfo) returns (stream AudioInfo) {}
}

message AudioInfo {
  string clientId = 1;
  repeated int32 samples = 2;
}